server:
  port: 8080

spring:
  application:
    name: eim-gateway
  profiles:
    active: dev
  main:
    allow-circular-references: true
    allow-bean-definition-overriding: true
eim-eim:
  version: eim-eim-version